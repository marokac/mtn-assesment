import { EllmentDirective } from './ellment.directive';

describe('EllmentDirective', () => {
  it('should create an instance', () => {
    const directive = new EllmentDirective();
    expect(directive).toBeTruthy();
  });
});
